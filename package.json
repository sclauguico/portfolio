{
  "name": "sailauguico-portfolio",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx --no-install astro dev",
    "start": "npx --no-install astro dev",
    "build": "npx --no-install astro check && npx --no-install astro build",
    "preview": "npx --no-install astro preview"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/tailwind": "^5.1.5",
    "astro": "^5.1.5",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.3"
  }
}
